name: Build and Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build, Test, Analyze
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x
        
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build Backend
      run: dotnet build UrlShortener.App.Backend/UrlShortener.App.Backend.csproj --configuration Release --no-restore
          
    - name: Build Frontend
      run: dotnet build UrlShortener.App.Blazor/UrlShortener.App.Blazor/UrlShortener.App.Frontend.csproj --configuration Release --no-restore

    - name: Test Backend
      run: dotnet test UrlShortener.Test.Backend/UrlShortener.Test.Backend.csproj

    - name: Test Frontend
      run: dotnet test UrlShortener.Test.Frontend/UrlShortener.Test.Frontend.csproj

    - name: Test End2End
      run: dotnet test UrlShortener.Test.E2E/UrlShortener.Test.E2E.csproj

    - name: Analyze App with SonarQube
      uses: SonarSource/sonarqube-scan-action@v5.0.0
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      with:
        args: >
          -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
          -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
